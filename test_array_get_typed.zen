{ io, Array, Option } = @std

main = () void {
    arr: Array<i32> = Array.new(10, 0)  
    arr.push(42)
    
    val: Option<i32> = arr.get(0)
    
    val ?
        | Option.Some(v) { io.println("Some: ${v}") }
        | Option.None { io.println("None") }
}
