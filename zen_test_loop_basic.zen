// Test basic loop with break
{ io } = @std

main = () void {
    counter ::= 0
    loop(() {
        io.println("In loop")
        counter = counter + 1
        
        // Simple break condition
        counter > 2 ? {
            break
        }
    })
    
    io.println("Loop finished")
}