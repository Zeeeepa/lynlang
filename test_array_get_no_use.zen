{ io, Array } = @std

main = () void {
    arr: Array<i32> = Array.new(10, 0)
    arr.push(42)
    
    // Just call get, dont use result
    _val = arr.get(0)
    io.println("Called get without crash")
}
