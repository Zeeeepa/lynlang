io := @std.io

main = (args: [string]) i32 {
    i := 0
    loop i < args.len() {
        arg := args[i]
        io.print("Arg: ")
        io.print(arg)
        io.print("\n")
        i = i + 1
    }
    return 0
}