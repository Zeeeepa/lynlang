// Test mutable assignment from LANGUAGE_SPEC.zen
{ io } = @std

main = () void {
    // Test mutable variable with ::=
    counter ::= 0
    io.print_int(counter)
    io.println("")
    
    // Try to modify it
    counter = 1
    io.print_int(counter)
    io.println("")
}