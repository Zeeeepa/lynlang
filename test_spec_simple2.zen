// Simple test of key Zen features

Point: {
    x: f64,
    y: f64,
}

main = () void {
    // Immutable with =
    x = 42
    
    // Mutable with ::=
    counter ::= 0
    counter = counter + 1
    
    // Struct creation
    p = Point { x: 1.0, y: 2.0 }
}