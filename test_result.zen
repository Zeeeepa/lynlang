// Test Result<T,E> type
main = () i32 {
    // Test with Result<i32, i32>
    success = Ok(42)
    result1 = success ?
        | Ok(val) { val }
        | Err(e) { 0 }
    
    // Test with Err variant
    failure = Err(13)
    result2 = failure ?
        | Ok(val) { 0 }
        | Err(e) { e }
    
    // Return sum to verify both work
    return result1 + result2
}