// Simple Option test without @std
main = () i32 {
    // Test with Option<f64>
    maybe_radius = Some(5.5)
    result = maybe_radius ?
        | Some(r) { 1 }
        | None { 0 }
    
    return result
}