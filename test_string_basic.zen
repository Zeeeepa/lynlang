// Test basic string operations with new type system
{ io } = @std

main = () i32 {
    // StringLiteral (compile-time)
    literal: string = "Hello"
    
    // StaticString 
    static_str: StaticString = "World"
    
    // Test string operations
    io.println(literal)
    io.println(static_str)
    
    return 0
}