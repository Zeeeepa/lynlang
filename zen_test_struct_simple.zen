// Test struct creation
{ io } = @std

// Simple struct
Point: {
    x: f64,
    y: f64,
}

main = () void {
    // Struct creation
    p = Point { x: 10.0, y: 20.0 }
    io.println("Created point")
}