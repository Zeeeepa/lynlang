// Test with direct literals to isolate the issue
extern printf = (format: *i8, ...) i32

test = () i32 {
    // Direct conditional without comparison
    x := true ? | true => 5 | false => 10
    return x
}

main = () i32 {
    result := test()
    printf("Result: %d\n", result)
    return 0
}