{ io, Option } = @std

main = () i32 {
    opt = Option.Some(42)
    
    // Without return - should fail
    result = opt ?
        | Option.Some(v) { v }
        | Option.None { 0 }
    
    io.println("This won't print")
    result
}