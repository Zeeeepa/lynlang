// Basic test of = for immutable and ::= for mutable

{ io } = @std

main = () void {
    // Test = for immutable assignment
    message = "Hello from Zen!"
    io.print(message)
    
    // Test ::= for mutable assignment
    counter ::= 0
    counter = counter + 1
    
    io.print("Done!")
}
