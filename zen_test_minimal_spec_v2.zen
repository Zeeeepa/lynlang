// Minimal test based on LANGUAGE_SPEC.zen

// Import
io = @std.io

// Simple struct
Point: {
    x: f64,
    y: f64,
}

// Trait - testing method syntax
Geometric: {
    area: (self) f64,
}

main = () void {
    io.println("Testing minimal spec")
}