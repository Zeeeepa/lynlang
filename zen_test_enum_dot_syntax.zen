// Test enum definitions with dot syntax from LANGUAGE_SPEC.zen

// Game entity enum from spec
GameEntity: .Player | .Enemy | .Powerup

// Import io for testing
{ io } = @std

// Test enum variant creation with dot syntax (as in spec)
main = () void {
    io.println("Testing enum dot syntax from LANGUAGE_SPEC.zen")
    
    // Test with GameEntity.Player syntax (as shown in spec)
    entity = GameEntity.Player
    io.println("Created GameEntity.Player (dot syntax)")
    
    // Test with shorthand .Variant syntax
    player = .Player
    io.println("Created .Player (shorthand syntax)")
    
    // Test another variant
    enemy = GameEntity.Enemy
    io.println("Created GameEntity.Enemy")
    
    io.println("Enum dot syntax test complete!")
}