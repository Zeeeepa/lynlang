{ io } = @std

// Define simple enum
Color: Red | Green | Blue

main = () void {
    c = Color.Red
    c ?
        | Red { io.println("Red") }
        | Green { io.println("Green") }
        | Blue { io.println("Blue") }
}
