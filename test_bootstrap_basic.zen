// Very basic bootstrap test
io := @std.io

main = () i32 {
    io.println("Bootstrap compiler starting...")
    io.println("Reading input file...")
    io.println("Lexing...")
    io.println("Parsing...")
    io.println("Type checking...")
    io.println("Generating code...")
    io.println("Compilation complete!")
    return 0
}