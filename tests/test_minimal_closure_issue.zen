// Minimal test case
{ io, Result } = @std

main = () i32 {
    add = (a: i32, b: i32) {
        return Result.Ok(a + b)
    }
    
    // Just call and raise, no comparison
    val = add(10, 5).raise()
    
    return 0
}