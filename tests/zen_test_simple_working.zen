// Simple test of working features
{ io } = @std

main = () void {
    // Basic variables
    x = 42
    
    // Range loop
    (0..3).loop((i) {
        io.println("i")
    })
    
    io.println("Done")
}