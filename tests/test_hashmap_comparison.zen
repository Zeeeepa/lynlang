// Test both HashMap constructors
{ io, HashMap } = @std

main = () void {
    io.println("Testing HashMap constructors")
    
    // Test 1: HashMap<i32, i32>.new()
    map1 = HashMap<i32, i32>.new()
    io.println("map1 created with .new()")
    
    // Test 2: HashMap<i32, i32>()
    map2 = HashMap<i32, i32>()
    io.println("map2 created with ()")
    
    // Now try insert on both
    io.println("Testing inserts...")
    
    // This should work
    map1.insert(1, 100)
    io.println("map1.insert worked")
    
    // This currently fails
    map2.insert(2, 200)
    io.println("map2.insert worked")
}