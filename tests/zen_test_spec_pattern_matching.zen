// Test pattern matching from LANGUAGE_SPEC.zen

main = () void {
    alloc = get_default_allocator()
    // Test boolean pattern matching (line 352-361)
    is_ready = true
    is_ready ? {
        // Single arm pattern match
        io.println("Ready!")
    }
    
    has_data = false
    has_data ?
        | true { io.println("Has data") }
        | false { io.println("No data") }
    
    // Test pattern matching with values
    value = 5
    value ?
        | 0 { io.println("Zero") }
        | 5 { io.println("Five") }
        | _ { io.println("Other") }
}