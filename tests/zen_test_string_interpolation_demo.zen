// Test string interpolation from LANGUAGE_SPEC.zen

main = () void {
    name = "Zen"
    value = 42
    pi = 3.14159
    
    // Test basic interpolation
    greeting = "Hello, ${name}!"
    message = "The answer is ${value}"
    math_msg = "Pi is approximately ${pi}"
}