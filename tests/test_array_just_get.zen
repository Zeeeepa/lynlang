{ io, get_default_allocator } = @std

main = () i32 {
    alloc = get_default_allocator()
    arr: Array<i32> = Array.new(alloc, 10, 0)
    arr.push(42)
    
    // Just call get, don't use the result
    arr.get(0)
    
    io.println("Done")
    return 0
}