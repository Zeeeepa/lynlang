// Test UFC from LANGUAGE_SPEC.zen
{ io } = @std

// Define a function that can be called as method
length = (s: string) i32 {
    return 5  // Placeholder
}

uppercase = (s: string) string {
    return "HELLO"  // Placeholder
}

main = () void {
    text = "hello"
    
    // Traditional function call
    len1 = length(text)
    io.println("Length (function): ${len1}")
    
    // UFC - function called as method
    len2 = text.length()
    io.println("Length (UFC): ${len2}")
    
    // Chain UFC calls
    result = text.uppercase()
    io.println("Uppercase: ${result}")
}