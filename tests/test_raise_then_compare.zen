// Test raise then comparison
{ io, Result , get_default_allocator} = @std

main = () i32 {
    alloc = get_default_allocator()
    get_five = () Result<f64, StaticString> {
        return Result.Ok(5.0)
    }
    
    val = get_five().raise()
    io.println("Got val: ${val}")
    
    // Now compare
    is_five = val == 5.0
    io.println("Is five: ${is_five}")
    
    return 0
}