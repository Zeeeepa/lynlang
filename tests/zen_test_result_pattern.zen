// Debug Result type test with pattern
{ io } = @std

Result<T, E>: Ok(T) | Err(E)

test = (n: i32) Result<i32, string> {
    val = Ok(n)
    return val
}

main = () void {
    result = test(5)
    io.println("Test passed")
}
