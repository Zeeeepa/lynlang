// Debug Result type test
{ io } = @std

// Result type definition
Result<T, E>: Ok(T) | Err(E)

// Simple function that creates Result directly
make_ok = () Result<i32, string> {
    return Ok(42)
}

main = () void {
    result = make_ok()
    io.println("Test passed")
}
