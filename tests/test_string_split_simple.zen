// Simple test for StaticString.split() method
{ io , get_default_allocator} = @std

main = () i32 {
    alloc = get_default_allocator()
    // Test basic split
    text = "hello,world"
    parts = text.split(",")
    
    io.println("Testing StaticString.split():")
    io.println("Split 'hello,world' by ','")
    
    // Just test that split works without crashing
    // Array methods need to be implemented separately
    
    // For now just test that it runs without crashing
    io.println("✓ StaticString.split() executed without crash")
    
    return 0
}