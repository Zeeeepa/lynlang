{ io, Result, Ok, Err } = @std

main = () i32 {
    // Test that void works as a value
    v = void
    io.println("void value created")
    
    // Test Result with void
    result = Ok(void)
    
    result ?
        | Ok(_) { io.println("Got Ok with void") }
        | Err(_) { io.println("Got Err") }
    
    0
}