{ io, Result } = @std

main = () i32 {
    // Test that void works as a value
    v = void
    io.println("void value created")
    
    // Test Result with void
    result = Result.Ok(void)
    
    result ?
        | Result.Ok(_) { io.println("Got Ok with void") }
        | Result.Err(_) { io.println("Got Err") }
    
    0
}