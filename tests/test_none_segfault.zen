// Minimal test to reproduce None segfault
{ io } = @std

main = () void {
    // Test with just None
    empty = None
    io.println("Created None")
    
    // Single pattern match on None
    empty ?
        | None { io.println("Is None") }
        | Some(x) { io.println("Is Some") }
}