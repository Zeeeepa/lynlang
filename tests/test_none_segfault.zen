// Minimal test to reproduce None segfault
{ io, Option } = @std

main = () void {
    // Test with just None
    empty = Option.None
    io.println("Created None")
    
    // Single pattern match on None
    empty ?
        | Option.None { io.println("Is None") }
        | Option.Some(x) { io.println("Is Some") }
}