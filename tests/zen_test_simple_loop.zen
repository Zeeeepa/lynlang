{ get_default_allocator } = @std

main = () void {
    alloc = get_default_allocator()
    // Simple loop() test
    loop(() {
        break
    })
    
    // Simple range with loop test
    nums = Vec<i32, 5>()
    nums.loop((n) {
        io.println("hi")
    })
}