// Simple test for StaticString.index_of() method
{ get_default_allocator } = @std

main = () i32 {
    alloc = get_default_allocator()
    text = "Hello, World!"
    
    // Simple test
    idx = text.index_of("World")
    io.println(idx)  // Should print 7
    
    // Not found test
    idx2 = text.index_of("Python")
    io.println(idx2)  // Should print -1
    
    return 0
}