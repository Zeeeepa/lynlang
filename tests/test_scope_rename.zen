// Test file to verify scope-aware rename
// This tests that renaming "value" in one function doesn't affect others

add = (a: i32, b: i32) i32 {
    value = a + b
    return value
}

multiply = (x: i32, y: i32) i32 {
    value = x * y
    return value
}

test_local_rename = () i32 {
    value = 42
    result = add(value, 10)
    return result
}

main = () i32 {
    value = 100
    x = add(5, 10)
    y = multiply(3, 4)
    return value
}
