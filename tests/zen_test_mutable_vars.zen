// Test mutable variables

{ io } = @std

main = () void {
    // Immutable binding
    x = 42
    
    // Mutable binding with ::=
    y ::= 100
    io.println("Initial y: 100")
    
    y = y + 1
    io.println("After increment y: 101")
}
