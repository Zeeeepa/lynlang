// Simple test to check basic language features
{ io , get_default_allocator} = @std

main = () void {
    alloc = get_default_allocator()
    io.println("Hello from Zen!")
    
    // Test variables
    x = 10
    y ::= 20
    io.println("x = ${x}, y = ${y}")
}