// Debug test for .raise() return value
{ io, Result } = @std

test_direct = () i32 {
    result = Result.Ok(42)
    value = result.raise()
    return value
}

main = () i32 {
    value = test_direct()
    io.println("Value: ${value}")
    return 0  // Return 0 for test success
}