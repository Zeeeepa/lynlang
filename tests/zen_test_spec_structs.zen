// Test struct definitions from LANGUAGE_SPEC.zen lines 116-120
// and lines 364-372

// Simple struct as in spec
Point: {
    x :: f64,    // mutable field  
    y :: f64 = 0 // with default value
}

// Another struct
Circle: {
    center: Point,
    radius: f64
}

main = () void {
    // Create struct instances (line 364-372)
    circle = Circle { 
        center: Point { x: 100, y: 100 }, 
        radius: 50 
    }
    
    // Access fields
    // TODO: Test pointer types when ready
}