// Test manual Option creation
{DynVec, get_default_allocator, Option, Some, None} = @std

main = () i32 {
    // Test pattern matching on manually created Option first
    opt = Some(42)
    
    opt ?
        | .Some(v) { return v }
        | .None { return 0 }
}