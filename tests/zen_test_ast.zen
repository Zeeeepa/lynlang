// Test AST generation

{ io , get_default_allocator} = @std

main = () void {
    alloc = get_default_allocator()
    // Test 1: direct call (not working)
    (0..2).loop((i) {
        io.println("Direct: ${i}")
    })
    
    // Test 2: assigned (works once) - Note: loop returns void so we don't assign
    (0..2).loop((j) {
        io.println("Assigned: ${j}")
    })
}