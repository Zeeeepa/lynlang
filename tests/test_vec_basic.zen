// Test Vec<T, size> basic operations
{ io } = @std

main = () void {
    // Create a Vec with fixed size
    vec = Vec<i32, 10>()
    vec.push(42)
    
    io.println("Vec created and pushed")
    
    // Test len
    length = vec.len()
    io.println("Vec length: ${length}")
}
