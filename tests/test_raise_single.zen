// Test single .raise() on nested Result
{ io, Result , get_default_allocator} = @std

// Function that returns Result<Result<i32, StaticString>, StaticString>
get_nested = () Result<Result<i32, StaticString>, StaticString> {
    Result.Ok(Result.Ok(42))
}

main = () i32 {
    alloc = get_default_allocator()
    // Test raise directly  
    result = get_nested().raise()
    io.println("After raise")
    
    // The result should now be Result<i32, StaticString>
    // But let's see what we actually get
    0
}