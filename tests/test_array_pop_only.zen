// Test just Array pop method
{io, Array, Option, get_default_allocator} = @std

main = () void {
    io.println("Testing Array pop")
    
    alloc = get_default_allocator()
    arr: Array<i32> = Array.new(alloc, 10, 0)
    arr.push(42)
    
    // Comment out pop for now
    // val = arr.pop()
    
    io.println("Length after push: ${arr.len()}")
    io.println("Done")
}
