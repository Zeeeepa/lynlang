// Simple test for .raise() from function call
{ io, Result, Ok, Err } = @std

get_result = () Result<i32, string> {
    return Ok(42)
}

main = () i32 {
    value = get_result().raise()
    io.println("Value: ${value}")
    return 0  // Return 0 for test success
}