{io, Option, get_default_allocator} = @std

main = () i32 {
    alloc = get_default_allocator()
    x = 42
    opt = Option.Some(x)
    opt ?
        | Some(val) { 
            io.println("${val}")
        }
        | None { io.println("None") }
    return 0
}
