// Test simple enum usage without pattern matching
{ io } = @std
{ Option } = @std.option

main = () void {
    // Just create enum variant
    value = Some(42)
    io.print("Created enum variant")
}
