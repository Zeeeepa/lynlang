// Simple UFC test

{ io } = @std

double = (n: i32) i32 {
    return n * 2
}

main = () void {
    // Traditional call
    result1 = double(5)
    io.println(result1)
    
    // UFC call - this should work
    num = 10
    result2 = num.double()
    io.println(result2)
}