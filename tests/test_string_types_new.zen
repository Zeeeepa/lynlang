// Test new StaticString type system: StaticString and String (no more confusing "StaticString")

main = () i32 {
    // Test 1: StaticString type (compile-time, no allocator)
    static_str: StaticString = "Hello, World!"

    // Test 2: String literals are implicitly StaticString
    literal = "This is a literal"  // Should be StaticString

    // Test 3: StaticString assignment
    another_static: StaticString = literal  // Should work

    // TODO: Test dynamic String when allocator is available
    // allocator := get_default_allocator()
    // dynamic_str: String = String.from_static("Dynamic StaticString", allocator)

    // If we got here, all type checks passed
    return 0
}