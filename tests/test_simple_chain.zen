// Simple test for method chaining
{ io, Result } = @std

get_result = () Result<i32, string> {
    Result.Ok(42)
}

main = () void {
    io.println("Testing simple method chain")
    
    // This should work - calling raise on function result
    value = get_result().raise()
    io.println("Value: ${value}")
}