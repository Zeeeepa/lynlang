// Minimal test for Some then None
{ io } = @std

main = () void {
    val = Some(42)
    empty = None
    
    val ?
        | Some(x) { io.println("Val: ${x}") }
        | None { }
    
    empty ?
        | Some(y) { io.println("Empty: ${y}") }
        | None { io.println("Empty is None") }
}