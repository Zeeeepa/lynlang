{ io } = @std

// Simple function returning Result<void, string>
test_fn = () Result<void, string> {
    Result.Ok(void)
}

main = () i32 {
    result = test_fn()
    
    result ?
        | Ok(_) { io.println("Got Ok") }
        | Err(e) { io.println("Got Err: ${e}") }
    
    0
}