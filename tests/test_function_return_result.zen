// Test function returning Result<i32, StaticString>
{ io, Result , get_default_allocator} = @std

// Function that returns Result<i32, StaticString>
make_result = (val: i32) Result<i32, StaticString> {
    Result.Ok(val)
}

main = () i32 {
    alloc = get_default_allocator()
    io.println("=== Testing function returns with Result<i32, StaticString> ===")
    
    // Call function
    res = make_result(42)
    
    // Extract value
    res ?
        | Result.Ok(v) { 
            io.println("Value from function: ${v}")
            io.println("Expected: 42")
        }
        | Result.Err(e) { io.println("Error: ${e}") }
    
    0
}