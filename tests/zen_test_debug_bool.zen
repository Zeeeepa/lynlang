// Debug test for boolean handling
{ io } = @std

main = () void {
    // Test boolean storage and retrieval
    is_ready = true
    io.println(is_ready)
    
    not_ready = false
    io.println(not_ready)
}