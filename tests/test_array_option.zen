// Test that Option.None works but Array.new doesn't
{ io } = @std

main = () i32 {
    io.println("Testing Option and Array")
    
    // This works
    none_val ::= Option.None
    io.println("Option.None works")
    
    // This should work too but doesn't 
    // arr ::= Array.new(5, 0)
    // io.println("Array created")
    
    return 0
}