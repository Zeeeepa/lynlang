// Test proper distinction between StaticString and String types

main = () i32 {
    // Test 1: String literals should be StaticString
    static_str: StaticString = "Hello, World!"
    
    // Test 2: Check StaticString literal type compatibility 
    literal = "This is a literal"  // Should be StaticString
    
    // Test 3: String type compatibility
    str: StaticString = "Old style StaticString"  // Should still work (maps to StaticString)
    
    // Test 4: Type compatibility for variable assignment
    another_static: StaticString = literal  // Should work
    
    // Test 5: Concatenation (when supported)
    // concat_result = static_str ++ " More text"
    
    // TODO: Test dynamic String (requires allocator)
    // When String implementation is ready:
    // allocator := get_default_allocator()
    // dynamic_str := String.from_static("Dynamic StaticString", allocator)
    
    // If we got here, all type checks passed
    return 0
}