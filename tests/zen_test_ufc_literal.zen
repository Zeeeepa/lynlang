// Simple UFC test on literal
{ io } = @std

double = (n: i32) i32 { return n * 2 }

main = () void {
    // Test UFC on integer literal directly - this might not parse
    result = double(10)  // Use regular call instead
    io.println("Result: ${result}")
}