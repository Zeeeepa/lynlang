// Test @this.defer statement
{ io } = @std

test_defer = () void {
    io.println("1. Start")
    @this.defer(io.println("3. Deferred"))
    io.println("2. End")
}

main = () void {
    io.println("Testing @this.defer:")
    test_defer()
    io.println("Done")
}