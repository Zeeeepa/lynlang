{ io , get_default_allocator} = @std

main = () i32 {
    alloc = get_default_allocator()
    x = (10 + 5)
    io.println("${x}")
    
    y = true
    y ? 
        | true { io.println("yes") }
        | false { }
    
    return 0
}