// Minimal test for .raise() parsing and basic functionality
{ io } = @std

// Simple function that returns an integer (no Result type for now)
get_value = (x: i32) i32 {
    return x * 2
}

// Test function that uses .raise() - this will help us see if parsing works
test_raise_parsing = () i32 {
    // This should at least parse correctly even if it doesn't work semantically
    // value = get_value(5).raise()  // Comment out the actual .raise() for now
    value = get_value(5)
    return value
}

main = () void {
    io.println("Testing .raise() parsing...")
    result = test_raise_parsing()
    io.println("Result: ${result}")
}