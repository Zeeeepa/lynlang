// Test HashMap<K,V> and HashSet<T> instantiation and basic operations
{ io } = @std

main = () i32 {
    io.println("Testing HashMap<K,V> instantiation...")
    
    // Test 1: Create HashMap with string keys and i32 values
    map1 ::= HashMap<string, i32>.new()
    io.println("✓ HashMap<string, i32> created")
    
    // Test 2: Create HashMap with i32 keys and string values
    map2 ::= HashMap<i32, string>.new()
    io.println("✓ HashMap<i32, string> created")
    
    // Test 3: Create HashSet with string elements
    set1 ::= HashSet<string>.new()
    io.println("✓ HashSet<string> created")
    
    // Test 4: Create HashSet with i32 elements
    set2 ::= HashSet<i32>.new()
    io.println("✓ HashSet<i32> created")
    
    io.println("\nAll generic instantiations successful!")
    
    return 0
}