{ io } = @std

// Option type from spec
Option<T>: Some(T) | None

main = () void {
    maybe = Option.Some(42)
    maybe ?
        | Some(val) { 
            io.print_int(val)
            io.println("")
        }
        | None { io.println("Nothing") }
}
