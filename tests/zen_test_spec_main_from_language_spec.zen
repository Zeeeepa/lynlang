// Testing working features from LANGUAGE_SPEC.zen
{ io } = @std

main = () void {
    // Test variable declarations (spec lines 299-306)
    // Only testing what currently works
    
    // 1. Forward declaration with type
    x: i32
    x = 10
    io.println(x)
    
    // 2. Immutable assignment (inferred)  
    y = 10
    io.println(y)
    
    // 3. Immutable with type
    z : i32 = 20
    io.println(z)
    
    // Test booleans
    is_ready: bool = true
    io.println(is_ready)
    
    another = false  
    io.println(another)
    
    // Test basic arithmetic
    sum = 10 + 20
    io.println(sum)
    
    product = 5 * 6
    io.println(product)
    
    // Test strings
    greeting = "Hello, Zen!"
    io.println(greeting)
}