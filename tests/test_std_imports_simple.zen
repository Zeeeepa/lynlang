// Test basic @std imports
{ Option, Some, None } = @std

main = () i32 {
    // Create an Option value
    x = Some(42)
    
    // Match on it
    result = x ?
        | .Some(val) { val }
        | .None { 0 }
    
    result
}