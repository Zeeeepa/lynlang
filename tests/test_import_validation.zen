// Test file for import validation
// This file should compile successfully as it uses correct import syntax

// Correct: Module-level imports
io := @std.io
math := @std.math

// Comptime blocks should be used for meta-programming only
comptime {
    // This is valid - generating compile-time constants
    CONSTANT := 42
}

// Simple test function
test_imports = () bool {
    // This demonstrates that imports work at module level
    return true
}

main = () i32 {
    // For now, just return success
    // This file compiles successfully with imports at module level
    return 0
}