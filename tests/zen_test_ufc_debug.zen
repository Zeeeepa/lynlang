// Debug UFC parsing issue
{ io } = @std

double = (n: i32) i32 {
    return n * 2
}

main = () void {
    io.println("UFC test")
    
    // Traditional call works
    result1 = double(5)
    io.println(result1)
    
    // UFC style - test parsing
    result2 = 5.double()
    io.println(result2)
}