// Minimal Option extraction test
{ io , get_default_allocator} = @std

main = () i32 {
    alloc = get_default_allocator()
    x = Some(42)
    
    // Just test pattern without using variable
    result = x ? 
        | .Some(_) => 999
        | .None => -1
    
    io.println("Result: ${result}")
    
    0
}