// Test enum pattern matching from LANGUAGE_SPEC.zen
{ io } = @std
{ Option } = @std.option

// Enum definitions from spec - using stdlib

main = () void {
    // Test Option enum creation and matching (line 448)
    maybe_value: Option<i32> = Some(42)
    maybe_value ?
        | Some(v) {
            io.println("Has value: ${v}")
        }
        | None {
            io.println("No value")
        }
    
    // Test None variant
    empty: Option<i32> = None
    empty ?
        | Some(_) { io.println("Should not print") }
        | None { io.println("Empty as expected") }
}