{ io, Result } = @std

main = () void {
    // Test 1: Nested Result creation
    outer = Result.Ok(Result.Ok(42))
    io.println("Created nested Result")
    
    // Test 2: First level raise
    inner = outer.raise()  
    io.println("First raise succeeded")
    
    // Test 3: Pattern matching on nested Result
    result = outer ? 
        | Result.Ok(inner_result) { 
            inner_result ? 
                | Result.Ok(value) { value }
                | Result.Err(e) { 99 }
        }
        | Result.Err(e) { 98 }
    
    io.println("Pattern matching result: ${result}")
}
