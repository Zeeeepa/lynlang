{ Result } = @std

divide = (a: f64, b: f64) Result<f64, StaticString> {
    return Result.Ok(5.0)
}

greet = (name: StaticString) void {
    println("Hello")
}

main = () i32 {
    // Test signature help - cursor inside println()
    println("testing")

    // Test inlay hints - type should be inferred
    result = divide(10.0, 2.0)

    // Test rename - select 'result' and rename
    result ?
        | Ok(val) { }
        | Err(msg) { }

    return 0
}
