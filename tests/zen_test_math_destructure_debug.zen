// Debug test for math destructuring
{ io } = @std

main = () void {
    io.println("Starting test...")
    
    // Do another destructuring import in the function
    { math } = @std
    
    io.println("After destructuring import")
    
    // Try to use math
    pi_val = 3.14159
    io.println("Pi value: ${pi_val}")
}