// Test Option pattern matching with explicit casting
{ io , get_default_allocator} = @std

main = () i32 {
    alloc = get_default_allocator()
    // Test Some with payload extraction
    x = Some(42)
    
    result = x ? 
        | .Some(v) => v  // Use the extracted value
        | .None => 99
    
    io.println("Result: ${result}")
    
    0
}