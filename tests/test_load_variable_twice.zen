// Test loading a variable twice
{ io , get_default_allocator} = @std

main = () void {
    alloc = get_default_allocator()
    x = 5
    io.println("x = ${x}")
    io.println("x again = ${x}")
    
    // Now use it in arithmetic
    y = x + 1
    io.println("y = ${y}")
}