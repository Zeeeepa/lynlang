// Test simple enum with function
{ io } = @std

// Function that takes enum parameter
test_enum = (val: bool) void {
    val ? {
        io.println("True branch")
    }
}

main = () void {
    v = true
    test_enum(v)
}