{ io , get_default_allocator} = @std

// Test calling strtod directly - currently inline.c is not fully implemented
// This test is disabled until inline.c FFI support is complete

main = () void {
    alloc = get_default_allocator()
    io.println("inline.c FFI test - not yet implemented")
    // Once working, this would test:
    // test_strtod = () f64 {
    //     inline.c("""
    //         const char* str = "3.14159";
    //         return strtod(str, NULL);
    //     """)
    // }
    // result = test_strtod()
    // io.println("strtod result: ${result}")
}