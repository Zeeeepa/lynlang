// Test just enum declaration and usage
{ io } = @std

// Simple enum with : syntax
MyEnum: .First | Second

main = () void {
    // Test creating enum variant
    val = MyEnum.First
    io.println("Created enum variant")
}