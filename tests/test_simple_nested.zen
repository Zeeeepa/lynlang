// Test simple nested generic
{ io, Result } = @std

main = () i32 {
    io.println("Testing nested Result")
    
    // Create inner Result
    inner = Result.Ok(42)
    io.println("Created inner: Result.Ok(42)")
    
    // Create outer Result
    outer = Result.Ok(inner)
    io.println("Created outer: Result.Ok(inner)")
    
    0
}