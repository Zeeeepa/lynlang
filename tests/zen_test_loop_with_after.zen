// Test loop with statement after
io = @std.io

main = () void {
    io.println("Before loop")
    
    loop(() {
        io.println("In loop")
        break
    })
    
    io.println("After loop")
}