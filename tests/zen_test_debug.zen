{ io } = @std

main = () void {
    // Test just the problematic part
    counter ::= 0
    loop(() {
        counter = counter + 1
        test = counter > 2
        test ?
            | true { break }
            | false { }
    })
    io.println("Done")
}