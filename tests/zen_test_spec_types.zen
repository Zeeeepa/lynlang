// Test type definitions from LANGUAGE_SPEC.zen

// Import Result and Option from stdlib
{ Result, Ok, Err, Option, Some, None } = @std

// Simple struct (line 116-120)
Point: {
    x:: f64,     // mutable field
    y:: f64 = 0  // with default value
}

// Main function
main = () void {
    // Test Option handling
    maybe_radius: Option<f64> = Some(5.5)
    
    // Test struct creation
    point = Point { x: 1.0, y: 2.0 }
}