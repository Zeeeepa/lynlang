// Debug test for Result<T,E> variable scope issue
{ io, Result } = @std

test_simple = () Result<i32, string> {
    a = Result.Ok(42)
    val = a.raise()
    return Result.Ok(val * 2)
}

main = () i32 {
    r = test_simple()
    io.println("Test complete")
    return 0
}