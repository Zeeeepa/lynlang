// Simple test to debug parsing
{ io , get_default_allocator} = @std

main = () void {
    alloc = get_default_allocator()
    io.println("Before hashmap")
    map = 42
    io.println("map created: ${map}")
    // Just try to call insert as a function first
    insert(map, 1, 100)
    io.println("Done")
}