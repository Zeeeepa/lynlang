// Minimal test based on LANGUAGE_SPEC.zen
// Tests core features that should work

{ io } = @std

// Simple struct
Point: {
    x:: f64, // mutable field
    y:: f64 = 0 // with default value
}

// Basic function
main = () void {
    // Immutable assignment
    x = 10
    y = 20
    
    // Mutable assignment
    z ::= 30
    
    // Basic boolean pattern matching
    is_ready = true
    is_ready ? {
        io.println("Ready!")
    }
    
    // Create struct
    p = Point { x: 1.0, y: 2.0 }
    
    // Basic output
    io.println("Test passed")
}