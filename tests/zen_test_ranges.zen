// Test range loops

{ io } = @std

main = () void {
    // Basic range loop
    (0..3).loop((i) {
        io.println("Count: 0,1,2")
    })
    
    // Range with step
    (0..10).step(5).loop((i) {
        io.println("Step: 0,5")
    })
}
