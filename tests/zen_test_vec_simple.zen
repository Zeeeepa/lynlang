// Test Vec<T, N> fixed-size vector
{ io } = @std

main = () void {
    // Create a fixed-size vector with size 5
    vec = Vec<i32, 5>()
    io.println("Created Vec<i32, 5>")
    
    // TODO: Test push and index operations once implemented
    // vec.push(10)
    // vec.push(20)
    // vec.push(30)
    // 
    // first = vec[0]
    // io.print("vec[0] = ")
    // io.print_int(first)
    // io.println("")
    
    io.println("Vec test complete")
}
