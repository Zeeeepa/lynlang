// Test capturing loop result

{ io , get_default_allocator} = @std

main = () void {
    alloc = get_default_allocator()
    (0..3).loop((i) {
        io.println("i = ${i}")
    })
    io.println("Loop complete")
}