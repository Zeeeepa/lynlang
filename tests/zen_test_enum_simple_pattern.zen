// Test simple enum pattern

Option<T>: Some(T) | None

main = () void {
    maybe = Some(42)
    
    // Pattern match without new variables
    maybe ?
        | Some(val) { x = 1 }
        | None { x = 2 }
}
