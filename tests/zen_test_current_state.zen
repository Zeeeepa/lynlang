// Test current working features based on LANGUAGE_SPEC.zen
{ io } = @std

main = () void {
    // Variable declarations (lines 298-306 in spec)
    x :: i32    // Mutable forward declaration
    x = 10
    y = 20           // Immutable
    z: i32 = 30     // Typed immutable  
    w ::= 40        // Mutable
    v :: i32 = 50   // Typed mutable
    
    io.println("Variables test passed")
    
    // Pattern matching (lines 352-361)
    is_ready = true
    is_ready ? {
        io.println("Pattern match test passed")
    }
    
    // Range loop (lines 432-434)  
    (0..3).loop((i) {
        io.println("Loop iteration: ${i}")
    })
    
    io.println("All tests passed!")
}