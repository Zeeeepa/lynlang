{ io } = @std

main = () i32 {
    // Create a simple Option<i32>
    val: i32 = 42
    opt: Option<i32> = Option.Some(val)
    
    opt ?
        | Option.Some(x) { io.println("Got: ${x}") }
        | Option.None { io.println("Got None") }
    
    return 0
}