// Test math module access
{ io } = @std

main = () void {
    io.println("Testing math module access...")
    
    // Try to access math directly  
    math = @std.math
    
    // This should work if math is loaded
    io.println("Math module loaded")
}