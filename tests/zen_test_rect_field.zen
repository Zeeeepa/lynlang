// Test just accessing struct field
{ io } = @std

Point: {
    x: f64,
    y: f64,
}

Rectangle: {
    top_left: Point,
}

main = () void {
    rect = Rectangle {
        top_left: Point { x: 1.0, y: 2.0 },
    }
    
    // Just access the field, don't print it
    tl = rect.top_left
    
    io.println("Got top_left field")
}