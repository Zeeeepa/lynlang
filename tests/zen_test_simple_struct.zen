{ io } = @std

Point: {
    x:: f64,
    y:: f64
}

main = () void {
    p = Point { x: 10.0, y: 20.0 }
    io.println("Point created")
    
    x_val = p.x
    y_val = p.y
    io.println("x = ${x_val}, y = ${y_val}")
}