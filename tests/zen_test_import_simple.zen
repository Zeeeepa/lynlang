// Simple test to verify import syntax works
// All imports at module level, not in comptime

core = @std
io = @std

main = () i32   {
    io.print("Import test: ")
    io.print("PASS\n")
    return 0
}