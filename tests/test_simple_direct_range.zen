// Test direct range creation without intermediate variables
{ io } = @std

main = () void {
    // Create range directly with literals
    range1 = 0..5
    io.println("Direct literal range created")
    
    // Create range with one variable
    x = 3
    range2 = 0..x
    io.println("Range with variable end created")
}