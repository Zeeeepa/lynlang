// Test Result<f64, string> type
{ io, Result } = @std

test_f64_result = () Result<f64, string> {
    return Result.Ok(3.14)
}

main = () void {
    result = test_f64_result()
    result ?
        | Result.Ok(val) { io.println("Got f64 value") }
        | Result.Err(e) { io.println("Got error") }
}