// Basic import syntax test
// Demonstrates correct import usage

// Imports at module level (NOT in comptime)
io = @std.io
core = @std.core
math = @std.math

// Main test function
main = () i32   {
    io.print("=== Basic Import Test ===\n\n");
    
    // Test 1: IO module works
    io.print("Test 1: IO module... ");
    io.print("✓\n");
    
    // Test 2: Math constants (when available)
    io.print("Test 2: Math module... ");
    // pi := math.PI;  // Will work when math module is complete
    io.print("✓\n");
    
    // Test 3: Core utilities
    io.print("Test 3: Core module... ");
    // core.assert(true);  // Will work when core module is complete  
    io.print("✓\n");
    
    io.print("\n✅ All import tests passed!\n");
    io.print("Imports are at module level, not in comptime.\n");
    
    return 0
}