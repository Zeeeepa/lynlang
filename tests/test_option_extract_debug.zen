// Test Option pattern matching with payload extraction - debug version
{ io } = @std

main = () i32 {
    // Test Some with payload extraction
    x = Some(42)
    
    result = x ? 
        | .Some(v) => v
        | .None => -1
    
    io.println("Extracted: ${result}")
    
    0
}