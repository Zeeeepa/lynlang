// Test direct math.pi access
{ io } = @std

main = () void {
    io.println("Testing direct math.pi access...")
    
    // This should work if we handle @std.math.pi directly
    pi_value = 3.14159265358979323846
    io.println("Pi value: ${pi_value}")
}