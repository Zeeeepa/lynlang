// Test StaticString vs String type distinction

main = () i32 {
    // Test 1: StaticString literals
    static1: StaticString = "Hello, World!"
    static2: StaticString = "Another static string"

    // Test 2: String literals are implicitly StaticString
    implicit_static: StaticString = "This should be StaticString"

    // Test 3: Variables of StaticString type
    var1: StaticString = static1
    var2: StaticString = implicit_static

    // Test 4: Passing StaticString to functions (when supported)
    // print_static(static1)

    // Note: Dynamic String requires stdlib and allocator, tested separately

    return 0
}