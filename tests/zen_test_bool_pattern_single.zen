// Test single-branch boolean pattern matching
{ io } = @std

main = () void {
    is_ready = true
    is_ready ? {
        io.println("Ready!") 
    }
    
    is_false = false
    is_false ? {
        io.println("This should not print")
    }
    
    io.println("Done")
}