// Test simple conditional with block
{ io , get_default_allocator} = @std

main = () i32 {
    alloc = get_default_allocator()
    x = 5.0
    
    // This should work - both arms return void
    x == 5.0 ?
        | true { io.println("x is 5.0") }
        | false { io.println("x is not 5.0") }
    
    return 0
}