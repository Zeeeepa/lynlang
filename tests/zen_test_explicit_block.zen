// Test explicit block compilation
{ io } = @std

main = () void {
    // Test block as statement
    {
        io.println("This is in a block")
    }
    
    // Test variable with block value
    x = {
        io.println("Block being evaluated")
        42
    }
    
    io.println("After blocks")
}