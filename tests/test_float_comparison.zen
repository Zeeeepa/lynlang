// Test simple float comparison
{ io } = @std

main = () i32 {
    b = 0.0
    result = b == 0.0
    result ? 
        | true { io.println("Equal") }
        | false { io.println("Not equal") }
    return 0
}