// Simple struct test

Point: {
    x :: f64,
    y :: f64 = 0
}

main = () void {
    p = Point { x: 10.0, y: 20.0 }
    io.println("Point created")
    
    // Try to access field
    val = p.x
    io.println("Field accessed")
}