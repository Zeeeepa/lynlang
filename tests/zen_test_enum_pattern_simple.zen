// Test enum pattern matching
io = @std.io

// Simple enum
Target: C | LLVM | Native

main = () void {
    target = Target.Native
    
    target ?
        | C { io.println("C backend") }
        | LLVM { io.println("LLVM backend") }
        | Native { io.println("Native backend") }
}