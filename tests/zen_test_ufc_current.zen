// Test current UFC implementation
{ io } = @std

// Define a simple function
length = (s: string) i32 {
    return 5  // Dummy implementation
}

main = () void {
    text = "hello"
    
    // Traditional function call
    len1 = length(text)
    io.println("Traditional: ${len1}")
    
    // UFC method call (text.length())
    len2 = text.length()
    io.println("UFC: ${len2}")
}