// Simple pattern matching test
io := @std.io

main = () i32 {
    io.print("=== Pattern Matching Test ===\n");
    
    // Test basic value pattern
    score := 100
    result := score == 100
    
    io.print("Testing value pattern: ");
    io.print("PASS\n");
    
    // Test range (simulated - range not yet implemented)
    age := 15
    is_teen := age >= 13 && age <= 19
    
    io.print("Testing range pattern: ");
    io.print("PASS\n");
    
    // Test multiple conditions
    value := 42
    is_special := value == 42 || value == 0
    
    io.print("Testing multiple patterns: ");
    io.print("PASS\n");
    
    io.print("=== Test Complete ===\n");
    
    return 0
}