{ io } = @std

main = () void {
    x = true
    x ?
        | true { io.println("x is true") }
        | false { io.println("x is false") }
    
    y = false  
    y ?
        | true { io.println("y is true") }
        | false { io.println("y is false") }
}