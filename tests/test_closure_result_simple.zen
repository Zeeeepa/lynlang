// Simple test to debug closure returning Result
{ io, Result , get_default_allocator} = @std

main = () i32 {
    alloc = get_default_allocator()
    // Simple closure returning Result<i32, StaticString>
    get_int = () {
        return Result.Ok(42)
    }
    
    result = get_int()
    io.println("Result type should be Result<i32,E>")
    
    0
}