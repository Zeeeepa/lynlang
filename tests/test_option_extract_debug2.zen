// Option with extraction
{ io } = @std

main = () i32 {
    x = Some(42)
    
    // Use x directly without assignment
    x ? 
        | Some(v) => {
            io.println("Value is: ${v}")
        }
        | None => {
            io.println("No value")
        }
    
    0
}