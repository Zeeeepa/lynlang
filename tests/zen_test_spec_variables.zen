// Test variable declarations from LANGUAGE_SPEC.zen lines 298-306
{ io } = @std

main = () void {
    io.println("=== Variable Declarations (LANGUAGE_SPEC.zen:298-306) ===")
    
    // Forward declaration is not yet implemented
    // x: i32
    // x = 10
    x = 10
    io.println("Immutable x = ${x}")
    
    // Immutable assignment (line 301)
    y = 20
    io.println("Immutable y = ${y}")
    
    // Immutable with type (line 302)
    z: i32 = 25
    io.println("Immutable with type z = ${z}")
    
    // Mutable forward declaration is not yet implemented
    // w:: i32
    // w = 30
    w ::= 30
    io.println("Mutable w = ${w}")
    w = 35
    io.println("After mutation w = ${w}")
    
    // Mutable assignment (line 305)
    v ::= 40
    io.println("Mutable v = ${v}")
    v = 45
    io.println("After mutation v = ${v}")
    
    // Mutable with type (line 306)
    u:: i32 = 50
    io.println("Mutable with type u = ${u}")
    u = 55
    io.println("After mutation u = ${u}")
    
    io.println("=== All variable tests passed ===")
}