// Test basic struct from spec
{ io } = @std

// Simple struct from spec line 117
Point: {
    x:: f64,  // mutable field
    y:: f64 = 0  // with default value
}

main = () void {
    // Create a Point
    p = Point { x: 10.0, y: 20.0 }
    
    io.println("Point created")
    io.println(p.x)
    io.println(p.y)
}