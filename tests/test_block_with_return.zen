// Test block with return statement
{ io } = @std

main = () i32 {
    x = 42
    
    // This block has a return, so it should be void
    check_result = x == 42 ? {
        io.println("x is 42")
        return 0
    }
    
    // If we get here, x was not 42
    io.println("x was not 42")
    return 1
}