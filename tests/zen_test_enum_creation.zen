// Test enum variant creation

{ Option } = @std.option

main = () void {
    // Try using enum literal directly
    maybe = Some(42)
    
    // Pattern match
    maybe ?
        | Some(val) { x = val }
        | None { x = 0 }
}