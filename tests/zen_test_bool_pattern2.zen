// Test boolean pattern matching
{ io } = @std

main = () void {
    flag = true
    
    // Test with explicit true/false patterns
    flag ?
        | true { io.println("True!") }
        | false { io.println("False!") }
    
    io.println("Done")
}