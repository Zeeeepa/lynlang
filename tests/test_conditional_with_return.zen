// Test conditional with return in branch
{ io } = @std

main = () i32 {
    x = 5.0
    
    // One branch has return
    x == 5.0 ?
        | true { io.println("x is 5.0") }
        | false { 
            io.println("x is not 5.0")
            return 1
        }
    
    return 0
}