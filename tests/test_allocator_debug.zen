// Test allocator imports with debug output

{ io, GPA } = @std

main = () i32 {
    io.println("Test 1: io module works")
    
    io.println("Test 2: GPA imported")
    
    // Try to use GPA
    sync_alloc = GPA.init()
    
    io.println("Test 3: GPA.init() called")
    
    return 0
}