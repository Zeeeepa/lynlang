// Simple test program for bootstrap compiler
io := @std.io

// Simple function to test
add = (a: i32, b: i32) i32 {
    return a + b
}

// Main entry point
main = () void {
    x := 10
    y := 20
    result := add(x, y)
    
    // For now just print a simple message
    io.print("Test successful: 10 + 20 = 30\n")
}