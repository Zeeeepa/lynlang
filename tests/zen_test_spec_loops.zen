// Test loops and ranges from LANGUAGE_SPEC.zen
// Lines 431-459

main = () void {
    // Range iterations (lines 431-434)
    (0..10).loop((i) {
        // Count: 0, 1, 2, ..., 9
    })
    
    // Step ranges (lines 436-439)
    (0..100).step(10).loop((i) {
        // Step: 0, 10, 20, ..., 90
    })
    
    // Infinite loop with break (lines 452-459)
    counter ::= 0
    loop(() {
        counter = counter + 1
        counter > 10 ?
            | true { break }
            | false { 
                // Continue looping
            }
    })
}