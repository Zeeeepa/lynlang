{ io } = @std

main = () void {
    // Direct boolean literal - works
    io.println(true)
    io.println(false)
    
    // Explicitly typed - works 
    a: bool = true
    io.println(a)
    
    // Inferred type - broken?
    b = true
    io.println(b)
}
