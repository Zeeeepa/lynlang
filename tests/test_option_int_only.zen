// Minimal test for Option<i32> pattern matching
{ io , get_default_allocator} = @std

main = () i32 {
    alloc = get_default_allocator()
    // Test with integer payload only
    x = Some(42)
    x ?
        | .Some(v) {
            io.println("Value: ${v}")
        }
        | .None {
            io.println("None")
        }
    
    0
}