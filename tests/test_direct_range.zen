// Test using range directly without storing
{ io , get_default_allocator} = @std

main = () void {
    alloc = get_default_allocator()
    x = 3
    // Use range directly in loop
    (0..x).loop((i) {
        io.println("i = ${i}")
    })
}