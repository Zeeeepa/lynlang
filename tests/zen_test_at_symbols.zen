// Test @ symbols per LANGUAGE_SPEC.zen

// Test @std import
{ io } = @std

main = () void {
    // Test @this.defer
    @this.defer(io.println("Deferred message"))
    
    io.println("Main message")
}