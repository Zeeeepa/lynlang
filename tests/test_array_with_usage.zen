// Test Array<T> with some actual usage
{ io } = @std

main = () i32 {
    io.println("Testing Array<T> with usage")
    
    // Create an array with capacity 10
    arr ::= Array.new(10, 0)
    io.println("Array created with capacity 10")
    
    // Note: Array methods like push, pop, get, set are not yet implemented
    // For now we just verify that Array.new() works
    
    io.println("Array.new() test passed!")
    
    return 0
}