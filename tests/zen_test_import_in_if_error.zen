// This test should FAIL - imports not allowed inside if blocks

main = () i32   {
    x := 10
    
    x > 5 ? {
        // ERROR: Import inside if block
        io = @std
        io.print("This should not compile\n")
    }
    
    return 0
}