// Simple test for Array usage
{ io, Array } = @std

main = () void {
    // Try the simplest possible Array usage
    arr = Array<i32>()
    arr.push(42)
    
    val = arr.get(0)
    io.println("Value: ${val}")
}