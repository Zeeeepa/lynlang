// Test enum with io

{ io } = @std
{ Option } = @std.option

main = () void {
    maybe = Some(42)
    
    maybe ?
        | Some(val) { io.println("Got Some") }
        | None { io.println("Got None") }
}
