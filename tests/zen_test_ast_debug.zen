// Debug AST for range loop

{ io } = @std

main = () void {
    // This should create a Range expression with a MethodCall
    range = (0..3)
    range.loop((i) {
        io.println("i = ${i}")
    })
}