// Simple test for Vec<T, size> constructor syntax
// This should test the parser and basic type checking

main = () void {
    // Test Vec<i32, 5> constructor - should parse successfully
    numbers = Vec<i32, 5>()
    
    // Test Vec with different size
    floats = Vec<f64, 10>()
    
    // Test boolean vector
    flags = Vec<bool, 3>()
    
    // Simple success message - using literal string to avoid io dependency
    // In a complete implementation, this would use io.println
    println("Collection constructors parsed successfully")
}

// Mock println function to avoid external dependencies
println = (msg: string) void {
    // This is a placeholder - in reality would call LLVM printf
}