// Test string interpolation feature from LANGUAGE_SPEC.zen
{ io } = @std

main = () void {
    name = "Zen"
    count = 42
    
    // Test basic string interpolation (line 186, 387-394 in spec)
    message = "Hello ${name}, count is ${count}"
    io.println(message)
    
    // Test in direct println
    io.println("Direct interpolation: ${name}")
}