// Debug test for range types
{ io , get_default_allocator} = @std

main = () void {
    alloc = get_default_allocator()
    // Create a range variable first
    my_range = 0..5
    
    my_range.loop((i) {
        io.println("i = ${i}")
    })
    
    io.println("Test passed!")
}
