// Test Option extraction with print in arm
{ io } = @std

main = () i32 {
    x = Some(42)
    
    result = x ? 
        | .Some(val) => {
            io.println("Got value in Some")
            100
        }
        | .None => -1
    
    io.println("Result: ${result}")
    
    0
}