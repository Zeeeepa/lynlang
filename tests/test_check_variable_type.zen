// Check how variable type is determined
{ io } = @std

main = () void {
    x = 3  // Should be i32 by default
    y: i32 = x  // This should work if x is i32
    io.println("y = ${y}")
    
    z: i64 = 3  // Explicit i64
    r = 0..z  // This might work
    io.println("Created range with i64")
}