// Minimal pattern test  
io = @std.io

Shape: Circle | Square

main = () void {
    s = Shape.Circle
    
    // Test pattern matching - try without blocks first
    s ?
        | Circle { }
        | Square { }
}