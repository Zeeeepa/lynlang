// Test @std imports
{DynVec, get_default_allocator} = @std

main = () i32 {
    alloc = get_default_allocator()
    vec = DynVec<i32>(alloc)
    vec.push(42)
    
    item = vec.get(0)
    
    return 0
}