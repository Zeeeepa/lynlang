// Test simple ternary
{ io } = @std

main = () i32 {
    val = 21
    
    // Simple boolean ternary - no pattern match
    val == 21 ? {
        io.println("Yes, value is 21")
    }
    
    // Now the opposite
    val != 21 ? {
        io.println("No, value is not 21")
    }
    
    return 0
}