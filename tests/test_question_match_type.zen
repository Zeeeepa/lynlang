// Test QuestionMatch type inference
{ io, Option } = @std

main = () i32 {
    // Test without explicit type annotation
    opt1 = Option.None
    
    // This should return i32 from both branches
    res = opt1 ?
        | Option.Some(x) => x + 10
        | Option.None => 42
    
    io.println("Result: ${res}")
    
    0
}