// Test StaticString.split() method - returns Array<StaticString> but array methods not yet fully implemented
{io, Array, get_default_allocator} = @std

main = () i32 {
    alloc = get_default_allocator()
    // Test basic split - returns memory address of Array<StaticString>
    s ::= "hello,world,test"
    parts ::= s.split(",")  // Returns Array<StaticString> with 3 elements
    
    // The split works but we can't access individual elements yet
    // without proper array indexing methods
    io.println("String split compiles and returns Array structure")
    
    // More split tests
    s2 ::= "one two three"
    words ::= s2.split(" ")  // Returns Array<StaticString> with 3 elements
    
    s3 ::= "path/to/file"
    path_parts ::= s3.split("/")  // Returns Array<StaticString> with 3 elements
    
    io.println("All splits executed successfully")
    
    0
}