// Test pattern matching with return in branch
{ io , get_default_allocator} = @std

main = () i32 {
    alloc = get_default_allocator()
    val = 5.0
    
    // Pattern match with return in false branch
    val == 5.0 ?
        | true { io.println("Pass") }
        | false {
            io.println("Fail")
            return 1
        }
    
    return 0
}