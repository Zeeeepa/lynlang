// Ultra minimal test
io = @std.io

// Simple enum
Color: Red | Green | Blue

main = () void {
    // Test enum
    c: Color = Color.Red
    c ?
        | Red { io.println("Red") }
        | Green { io.println("Green") }
        | Blue { io.println("Blue") }
}