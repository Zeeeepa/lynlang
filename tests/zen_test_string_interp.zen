// Test string interpolation from LANGUAGE_SPEC.zen
{ io } = @std

main = () void {
    x = 42
    name = "Zen"
    
    // Test basic interpolation (line 186)
    io.println("The answer is ${x}")
    io.println("Hello, ${name}!")
    
    // Test in expressions (lines 387-394)
    message = "Count: ${x}"
    io.println(message)
}