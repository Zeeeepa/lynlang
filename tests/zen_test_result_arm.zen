// Debug Result type test with pattern arm return
{ io } = @std

Result<T, E>: Ok(T) | Err(E)

check = (n: i32) Result<i32, string> {
    n > 0 ?
        | true { return Ok(n) }
        | false { return Err("negative") }
}

main = () void {
    result = check(5)
    io.println("Test passed")
}
