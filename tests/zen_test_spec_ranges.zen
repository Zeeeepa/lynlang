// Test range loops from LANGUAGE_SPEC.zen lines 432-439
{ io } = @std

main = () void {
    io.println("=== Range Loops (LANGUAGE_SPEC.zen:432-439) ===")
    
    // Basic range loop (lines 432-434)
    io.println("\n1. Basic range (0..5):")
    (0..5).loop((i) {
        io.println("  Count: ${i}")
    })
    
    // Different range
    io.println("\n2. Range (10..15):")
    (10..15).loop((i) {
        io.println("  Value: ${i}")
    })
    
    // Using range values in calculations
    io.println("\n3. Using range values:")
    sum ::= 0
    (1..4).loop((i) {
        sum = sum + i
        io.println("  i=${i}, sum=${sum}")
    })
    io.println("Final sum: ${sum}")
    
    // Note: Step ranges (lines 437-439) are not yet implemented
    // Would be: (0..100).step(10).loop((i) { ... })
    
    io.println("\n=== All range tests passed ===")
}