// Test enum with pattern matching

Option<T>: Some(T) | None

main = () void {
    maybe = Some(42)
    
    // Pattern match with variable assignment
    maybe ?
        | Some(val) { result = val }
        | None { result = 0 }
}
