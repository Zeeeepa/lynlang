{ HashMap, DynVec, get_default_allocator } = @std

main = () void {
    // Missing allocator - should trigger diagnostic
    map = HashMap.new<StaticString, i32>()

    // Correct usage
    allocator = get_default_allocator()
    good_map = HashMap.new<StaticString, i32>(allocator)
}
