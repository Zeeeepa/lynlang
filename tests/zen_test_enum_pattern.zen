// Test enum with pattern matching

{ Option } = @std.option

main = () void {
    maybe = Some(42)
    
    // Simple pattern match
    maybe ?
        | Some(val) { result = val }
        | None { result = 0 }
}
