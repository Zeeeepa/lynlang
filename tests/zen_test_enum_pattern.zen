// Test enum with pattern matching

Option<T>: Some(T) | None

main = () void {
    maybe = Some(42)
    
    // Simple pattern match
    maybe ?
        | Some(val) { result = val }
        | None { result = 0 }
}
