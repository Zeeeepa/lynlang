// Test Option pattern matching with positive integers only
{ io } = @std

main = () i32 {
    // Test Some with payload extraction
    x = Some(42)
    
    result = x ? 
        | .Some(v) => v
        | .None => 99
    
    io.println("Extracted: ${result}")
    
    0
}