// Test pattern matching from spec
{ io } = @std

main = () void {
    // Boolean pattern matching - no ternary (spec line 352)
    is_ready = true
    is_ready ? { 
        io.println("Starting game!") 
    }
    
    // For if-else, use full pattern match (spec line 357)
    has_data = false
    has_data ?
        | true { io.println("Processing data...") }
        | false { io.println("Waiting for data...") }
}