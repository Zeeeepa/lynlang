{ io, get_default_allocator } = @std

main = () i32 {
    io.println("Test 1: Creating array")
    alloc = get_default_allocator()
    arr: Array<i32> = Array.new(alloc, 10, 0)
    io.println("Test 2: Array created successfully")
    
    return 0
}