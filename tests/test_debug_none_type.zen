// Debug None type inference
{ io } = @std

main = () i32 {
    // Test None without type annotation
    opt = Option.None
    
    // Try to use it in a match
    res = opt ?
        | Option.Some(x) => {
            io.println("Some: x type would be inferred here")
            42  // Return a concrete value
        }
        | Option.None => {
            io.println("None branch")
            0
        }
    
    io.println("Result: ${res}")
    
    0
}