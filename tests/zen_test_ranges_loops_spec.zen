// Test ranges and loops from LANGUAGE_SPEC.zen (lines 431-439)

{ io } = @std

main = () void {
    io.println("=== Testing Ranges and Loops from LANGUAGE_SPEC ===")
    
    // Basic range iteration (line 432-434)
    io.println("\n1. Basic Range (0..5):")
    (0..5).loop((i) {
        io.print("${i} ")
    })
    io.println("")
    
    // Step ranges not yet implemented
    io.println("\n2. Step Range - TODO: Not yet implemented")
    
    // Larger range
    io.println("\n3. Range (1..10):")
    (1..10).loop((i) {
        io.print("${i} ")
    })
    io.println("")
    
    // Test with variable bounds
    io.println("\n4. Variable Range:")
    start = 2
    end = 8
    (start..end).loop((i) {
        io.print("${i} ")
    })
    io.println("")
    
    io.println("\n=== Range/Loop Test Complete ===")
}