// Test current implementation against LANGUAGE_SPEC.zen
// Testing basic features that should work

{ io } = @std

main = () void {
    // Test immutable assignment (=)
    x = 10
    y = 20
    
    // Test @std.io.println
    io.println("Testing basic features:")
    io.println(x)
    io.println(y)
    
    // Test arithmetic
    sum = x + y
    io.println(sum)
    
    // Test boolean literals
    is_ready = true
    io.println(is_ready)
    
    // Test string literals
    message = "Hello from Zen!"
    io.println(message)
}