// Test direct loop vs functional loop

{ io } = @std

main = () void {
    // Direct loop statement (currently not supported per spec comment)
    counter ::= 0
    loop(() {
        (counter >= 3) ? { break }
        io.println("Loop: ${counter}")
        counter = counter + 1
    })
    
    io.println("Final: ${counter}")
}