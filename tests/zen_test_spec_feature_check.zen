// Check specific features from LANGUAGE_SPEC.zen
{ io, math } = @std

main = () void {
    io.println("=== Feature Check ===")
    
    // Test math.pi (should work per IMPLEMENTATION_STATUS)
    radius = 5.0
    area = math.pi * radius * radius
    io.println("Circle area with radius 5: ")
    io.println(area)
    
    // Test @this.defer (should work)
    io.println("Before defer setup")
    @this.defer(io.println("Deferred cleanup executed"))
    io.println("After defer setup")
    
    // Test Option type (line 110 in spec)
    // Option<T>: Some(T) | None
    // maybe: Option<i32> = Some(42)
    // io.println("Option test - commented out for now")
    
    io.println("=== End of tests ===")
}
