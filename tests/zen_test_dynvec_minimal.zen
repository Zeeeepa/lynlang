// Minimal DynVec test
{ io } = @std

main = () i32 {
    // Create a DynVec with initial capacity
    vec = DynVec<i32>(0, 10)
    
    // Test push
    vec.push(42)
    io.println("Pushed 42")
    
    // Test len
    length = vec.len()
    io.println("Length: ${length}")
    
    return 0
}