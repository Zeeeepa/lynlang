// Test referencing math after import
{ io, math } = @std

main = () void {
    io.println("Checking math module...")
    
    // Try to reference math - should not fail
    m = math  // This will fail if math is undefined
    
    io.println("Math module referenced successfully")
}