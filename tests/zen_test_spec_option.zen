// Test Option type from LANGUAGE_SPEC.zen line 109-110
// Option<T>: Some(T) | None

// Define the Option type as in spec
Option<T>: Some(T) | None

main = () void {
    // Test Option types
    maybe_value: Option<i32> = Option.Some(42)
    nothing: Option<i32> = Option.None
    
    // Pattern matching with ? operator (line 462-473)
    maybe_value ?
        | Some(val) {
            // We have a value
        }
        | None {
            // No value
        }
}