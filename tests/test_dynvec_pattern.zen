// Debug DynVec pattern matching issue
{DynVec, get_default_allocator, Option} = @std

main = () i32 {
    alloc = get_default_allocator()
    vec = DynVec<i32>(alloc)
    vec.push(42)
    
    // Get with pattern matching
    item = vec.get(0)
    
    item ?
        | .Some(v) { return v }
        | .None { return 0 }
}