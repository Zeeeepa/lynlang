{ io , get_default_allocator} = @std

main = () i32 {
    alloc = get_default_allocator()
    val = 42
    c = "*"
    
    io.println("Direct: ${val}")
    io.println("Char: ${c}")
    
    return 0
}
