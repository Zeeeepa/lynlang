// Test Option enum definition and usage

{ Option } = @std.option

main = () void {
    // Create an Option value
    maybe = Option.Some(42)
    
    // Pattern match on it
    maybe ?
        | Some(val) { x = val }
        | None { x = 0 }
}