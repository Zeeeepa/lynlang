{ io } = @std

main = () void {
    // Test different ways to call .loop on ranges
    
    // Direct range without parens - doesn't work due to precedence
    // Would need to be: (0..3).loop((i) { ... })
    // Commenting out for now
    
    // Range with parens - should work
    (0..3).loop((i) {
        io.println("With parens: ${i}")
    })
}