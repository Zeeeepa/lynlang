// Debug boolean interpolation
{ io } = @std

main = () void {
    // Test boolean literals directly
    io.println("Direct true: ${true}")
    io.println("Direct false: ${false}")
    
    // Test boolean variables
    yes = true
    no = false
    io.println("Variable yes: ${yes}")
    io.println("Variable no: ${no}")
}