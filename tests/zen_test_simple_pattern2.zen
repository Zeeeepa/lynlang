// Very simple pattern test
{ io } = @std

main = () void {
    io.println("Before pattern")
    
    true ?
        | true { 
            io.println("In true branch")
        }
        | false { 
            io.println("In false branch")
        }
    
    io.println("After pattern")
}