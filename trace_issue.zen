// Step by step
test1 = (n: i32) i32 {
    // Works: parenthesize the whole thing
    return (n > 5 ? | true => 10 | false => 20)
}

test2 = (n: i32) i32 {
    // Fails?: no parentheses
    return n > 5 ? | true => 10 | false => 20
}

main = () i32 {
    return 0
}