// Hello World in Zen
{ io } = @std


Person: {
    age: i32,
    name: StaticString,
}

main = () i32 {
    person = Person (
    age: 20,
    name: "John"
    )
    io.println("Hello, ${person.name}!")
    return 0
}
