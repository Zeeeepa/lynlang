// Zen Language Example: Hello World
// The simplest possible Zen program

// Bootstrap imports - no comptime needed
build := @std.build
io := build.import("io")

// Entry point - every Zen program needs a main function
main = () void {
    io.print("Hello, Zen World!\n")
}