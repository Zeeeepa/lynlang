// Test enum definitions with current :: syntax

// Game entity enum from spec
GameEntity: .Player | .Enemy | .Powerup

// Import io for testing
{ io } = @std

// Test basic enum usage with :: syntax (current implementation)
main = () void {
    io.println("Testing enum definitions")
    
    // Test with :: syntax that parser currently expects
    entity = GameEntity::Player
    io.println("Created GameEntity::Player")
    
    // Test shorthand .Variant syntax (should work)
    player = .Player
    io.println("Created .Player variant")
    
    io.println("Enum creation test complete!")
}