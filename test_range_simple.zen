// Simple range loop test
io = @std

main = () i32 {
    io.println("Testing simple range loop...")
    
    // Just test basic range loop  
    (0..3).loop((i) {
        io.print("i = ")
        io.print_int(i)
        io.println("")
    })
    
    io.println("Done!")
    return 0
}