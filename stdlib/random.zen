// Zen Standard Library: Random number generation

Rng: {
    seed: i64
}

seed_rng = (seed: i64) Rng {
    return Rng { seed: seed }
}

next_int = (rng: Rng) i64 {
    return 0
}

next_float = (rng: Rng) f64 {
    return 0.0
}
