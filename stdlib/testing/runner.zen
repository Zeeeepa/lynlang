// Test runner for Zen

TestResult:
    Pass,
    Fail,
    Skip

TestCase: {
    name: StaticString,
    passed: bool
}

TestSuite: {
    name: StaticString,
    count: i32
}

run_test = (name: StaticString) bool {
    return true
}
