// Zen Standard Library: Queue

Queue: {
    data: *u8,
    len: usize
}

queue_new = () Queue {
    return Queue { data: compiler.null_ptr(), len: 0 }
}

queue_enqueue = (queue: Queue, value: i32) void {
    return void
}

queue_dequeue = (queue: Queue) i32 {
    return 0
}
