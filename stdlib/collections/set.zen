// Zen Standard Library: Set

Set: {
    data: *u8,
    len: usize
}

set_new = () Set {
    return Set { data: compiler.null_ptr(), len: 0 }
}

set_insert = (set: Set, value: i32) void {
    return void
}

set_contains = (set: Set, value: i32) bool {
    return false
}
