// Collections module - all data structures
// Clean aggregator for all collection types

{ Vec, DynVec } = import("../vec.zen")
{ HashMap } = import("hashmap.zen")
{ Set } = import("set.zen")  
{ List } = import("list.zen")
{ Queue } = import("queue.zen")
{ Stack } = import("stack.zen")

module.exports = {
    Vec, DynVec,
    HashMap, Set, List,
    Queue, Stack,
}
