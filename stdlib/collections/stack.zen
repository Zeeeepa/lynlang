// Zen Standard Library: Stack

Stack: {
    data: *u8,
    len: usize
}

stack_new = () Stack {
    return Stack { data: compiler.null_ptr(), len: 0 }
}

stack_push = (stack: Stack, value: i32) void {
    return void
}

stack_pop = (stack: Stack) i32 {
    return 0
}
