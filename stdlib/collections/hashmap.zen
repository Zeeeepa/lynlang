// Zen Standard Library: HashMap

HashMap: {
    capacity: usize
}

hashmap_new = () HashMap {
    return HashMap { capacity: 0 }
}

hashmap_insert = (map: HashMap, key: StaticString, value: i32) void {
    return void
}

hashmap_get = (map: HashMap, key: StaticString) i32 {
    return 0
}
