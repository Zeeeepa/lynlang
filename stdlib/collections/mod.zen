// Collections module - re-exports all collection types
// Provides a unified interface to all data structures

{ Vec, DynVec } = import("../vec.zen")
{ HashMap } = import("hashmap.zen")
{ HashSet, Set } = import("set.zen")  
{ List } = import("list.zen")
{ Queue } = import("queue.zen")
{ Stack } = import("stack.zen")

module.exports = {
    Vec, DynVec,
    HashMap, HashSet, Set, List,
    Queue, Stack,
}
