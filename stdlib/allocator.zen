// BACKWARD COMPATIBILITY ONLY - Use memory_unified.zen
// This file maintained for existing code that imports allocator.zen

{ memory_unified } := @std

// Map old interface to new unified interface
Allocator = memory_unified.Allocator
DefaultAllocator = memory_unified.DefaultAllocator 
get_default_allocator = memory_unified.get_default_allocator

// Re-export all functions for compatibility
module.exports = memory_unified.exports
