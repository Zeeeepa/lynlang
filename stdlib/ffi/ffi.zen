// Zen Standard Library: FFI (Foreign Function Interface)

Library: {
    handle: *u8
}

load_library = (path: StaticString) *u8 {
    return compiler.null_ptr()
}

get_symbol = (lib: *u8, name: StaticString) *u8 {
    return compiler.null_ptr()
}

unload_library = (lib: *u8) void {
    return void
}
