// Test to see if the issue is with the assignment or the method call itself
{io, DynVec, get_default_allocator, Option} = @std

main = () i32 {
    alloc = get_default_allocator()
    vec = DynVec<i32>(alloc)
    vec.push(42)
    
    // Try without assignment
    vec.get(0) ?
        | Some(v) { io.print_int(v) }
        | None { io.println("None") }
    
    return 0
}