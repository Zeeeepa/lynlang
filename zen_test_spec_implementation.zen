// Test implementable subset of LANGUAGE_SPEC.zen features
// Focus on what we can get working now

// 1. Direct imports from @std
io = @std.io

// 2. Simple function - test basic functionality
main = () void {
    // Test print
    io.println("Testing LANGUAGE_SPEC.zen implementation")
    
    // Immutable variable (default with =)
    x = 42
    io.println("Immutable x = 42")
    
    // Mutable variable with ::=
    y ::= 100
    y = y + 1
    io.println("Mutable y after increment = 101")
}