// Test program with new import syntax

// Imports at module level - no comptime needed!
io := @std.io
core := @std.core

// Simple main function
main = () i32 {
    io.print("Hello from Zen!\n")
    io.print("✅ Imports work without comptime!\n")
    io.print("✅ Clean syntax: identifier := @module.path\n")
    
    return 0
}