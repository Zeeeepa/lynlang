{ io, Result } = @std

main = () i32 {
    // Test closure returning Result<f64, string>
    compute = () Result<f64, string> {
        return Result.Ok(3.14)
    }
    
    value = compute().raise()
    io.println("Value: ${value}")
    
    return 0
}
