math := build.import("math")
io := build.import("io")

distance = (x: f64, y: f64) f64 {
    return math.sqrt(x * x + y * y)
}

main = () f64 {
    a := 3.0
    b := 4.0
    dist := distance(a, b)
    return dist
}
