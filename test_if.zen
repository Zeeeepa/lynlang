io := @std.io

main = () i32 {
    x := 5
    y := 10
    
    if x < y {
        io.print("x is less than y\n")
    }
    
    if (x == 5) && (y == 10) {
        io.print("Both conditions are true\n")
    }
    
    return 0
}