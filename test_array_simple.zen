{ io, Array } = @std

main = () void {
    arr: Array<i32> = Array.new(10, 0)
    
    // Push one value
    arr.push(42)
    io.println("Pushed 42")
    
    len = arr.len()
    io.println("Length: ${len}")
}
