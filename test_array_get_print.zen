{ io, Array } = @std

main = () void {
    arr: Array<i32> = Array.new(10, 0)
    arr.push(42)
    
    val = arr.get(0)
    
    // Try to access it as raw struct
    io.println("Got value, about to print")
    io.println("Value: ${val}")
}
