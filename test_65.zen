{ io } = @std

main = () i32 {
    opt = Option.Some(65)  // ASCII "A"
    opt ?
        | Some(val) { 
            io.println("${val}")
        }
        | None { io.println("None") }
    return 0
}
