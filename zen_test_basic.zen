{ io } = @std

main = () void {
    // Test basic immutable variable
    x = 42
    io.print_int(x)
    io.println("")
    
    // Test mutable variable
    counter ::= 0
    counter = counter + 1
    io.print_int(counter)
    io.println("")
}
