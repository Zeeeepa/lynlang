// Simplest test case
extern printf = (format: *i8, ...) i32

test = () i32 {
    x := 1 > 0 ? | true => 5 | false => 10
    return x
}

main = () i32 {
    result := test()
    printf("Result: %d\n", result)
    return 0
}